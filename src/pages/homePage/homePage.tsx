import React from 'react';

import './homePage.css';

type Props = {};

const homePage: React.FC<Props> = (props) => (
  <div className="container">
    Тут будет два ребенка(Левая часть в виде кнопок и правая с контентом)
  </div>
);

export default homePage;
